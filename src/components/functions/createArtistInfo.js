//use extract name and picture from festival data
//extract link from spotify data
//combine into an artist object to add into artistTrackBox

export default function createArtistInfo({festivalData, matchedTracks}) {

}